{% extends "backend.html.twig" %}
{% block title %} ciudades {% endblock %}

{% block contenido %}

    <div class="sub-header">
        <div class="col-lg-3 btn-search">
            <div class="input-group">
                <input type="text" class="form-control">
                <span class="input-group-btn">
                    <a href=" path('sm_admin_ciudad_search') " class="btn btn-default" role="button">
                        Buscar
                    </a>
                </span>
            </div>
        </div>
        <div class="text-right">
            <a href="{{ path('sm_admin_ciudad_new') }}">
                <button type="button" class="btn btn-danger">Agregar una ciudad</button>
            </a>
        </div>
    </div>

    <div class="table-responsive">
        <table class='table table-striped table-hover table-bordered'>
            <thead>
                <tr>
                    <th>#</th>
                    <th>nombre</th>
                    <td>Acciones</td>
                </tr>
            </thead>

            <tbody>
                {% for ciudad in ciudadesPadinadas %}
                    <tr>
                        <td> {{ loop.index }}</td>
                        <td>{{ ciudad.nombre }}</td>
                        <td>
                            <div class="btn-group btn-group-sm">
                              <a href="{{ path('sm_admin_ciudad_edit', {'id': ciudad.id}) }}" class="btn btn-default" role="button">
                                <i class="fa fa-pencil-square-o"></i>
                              </a>
                              <a href="{{ path('sm_admin_ciudad_delete', {'id': ciudad.id} ) }}" class="btn btn-default" role="button">
                                <i class="fa fa-trash-o"></i>
                              </a>
                            </div>
                        </td>

                    </tr>
                {% endfor %}
            </tbody>
        </table>
        
        {# incluimos el paginador  #}
        {{ knp_pagination_render(ciudadesPadinadas) }}

    </div>

{% endblock %}